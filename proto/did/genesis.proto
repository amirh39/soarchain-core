syntax = "proto3";
package soarchain.did;
import "did/reputation.proto";
import "gogoproto/gogo.proto";
import "did/params.proto";
// this line is used by starport scaffolding # genesis/proto/import

option go_package = "soarchain/x/did/types";

// GenesisState defines the did module's genesis state.
message GenesisState {
  Params params = 1 [(gogoproto.nullable) = false];
  repeated string DidList = 2 [(gogoproto.nullable) = false];
  repeated Reputation reputationList = 3 [(gogoproto.nullable) = false];
  // this line is used by starport scaffolding # genesis/proto/state
}
