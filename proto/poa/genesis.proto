syntax = "proto3";
package soarchain.poa;

import "gogoproto/gogo.proto";
import "poa/params.proto";
import "poa/client.proto";
import "poa/challenger.proto";
// this line is used by starport scaffolding # genesis/proto/import

option go_package = "soarchain/x/poa/types";

// GenesisState defines the poa module's genesis state.
message GenesisState {
  Params params = 1 [(gogoproto.nullable) = false];
  repeated Client clientList = 2 [(gogoproto.nullable) = false];
  repeated Challenger challengerList = 3 [(gogoproto.nullable) = false];
  // this line is used by starport scaffolding # genesis/proto/state
}
